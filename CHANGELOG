0.0.2:

 - Accepted PR that changed the filepath from ~/.comcek/ to conform to the XDG spec, as per: https://specifications.freedesktop.org/basedir-spec/basedir-spec-latest.html
 - Added `cmk upath` to enable the end-user to find the path to store the "cmd.yaml" file